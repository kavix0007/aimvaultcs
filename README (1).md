Guide content lives in ../data/guides.json and renders through ../guide.html?slug=...

This folder is kept for anyone who prefers static per-guide HTML files later
(e.g. for a future pre-render step) — see the README's "Known limitations"
section for how that upgrade would work.
